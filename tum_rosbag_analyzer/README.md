# tum_rosbag analyzer

Analyze the timestamps of your favorite PointCloud2 message ❤️

## How to run

The general command to run this tool is:

```bash
ros2 run rosbag_analyzer rosbag_analyzer <mode> /path/to/rosbag /topic/name
```

Mode is either "timestamps" or "tbd"!
